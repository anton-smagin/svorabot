<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
    </style>
  </head>

  <body>
    <%= yield %>
    <p><%= t('mails.telegram_username', contact: @user.telegram_username) %></p>
    <p><%= t('mails.name', first_name: @user.first_name, last_name: @user.last_name) %></p>
    <p><%= t('mails.leaved_contact', contact: @user.contacts) %></p>
    <p><%= t('mails.leaved_instagram', instagram: @user.instagram) %></p>
    <p><%= t('mails.age', age: @user.age) %></p>
    <%= yield %>
  </body>
</html>
